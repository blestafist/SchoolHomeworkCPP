# 📚 Шпаргалка по `<algorithm>` C++ (GCC/G++)

## 30 самых полезных встроенных функций

| № | Функция | Описание | Сложность | Пример использования |
|---|---------|----------|-----------|----------------------|
| 1 | **sort()** | Сортирует элементы | O(n log n) | `sort(v.begin(), v.end());` |
| 2 | **binary_search()** | Бинарный поиск (требует отсортированный массив) | O(log n) | `binary_search(v.begin(), v.end(), 42);` |
| 3 | **lower_bound()** | Первый элемент ≥ значения | O(log n) | `auto it = lower_bound(v.begin(), v.end(), 5);` |
| 4 | **upper_bound()** | Первый элемент > значения | O(log n) | `auto it = upper_bound(v.begin(), v.end(), 5);` |
| 5 | **reverse()** | Разворачивает последовательность | O(n) | `reverse(v.begin(), v.end());` |
| 6 | **next_permutation()** | Следующая перестановка | O(n) | `next_permutation(v.begin(), v.end());` |
| 7 | **prev_permutation()** | Предыдущая перестановка | O(n) | `prev_permutation(v.begin(), v.end());` |
| 8 | **unique()** | Удаляет дубликаты (нужна сортировка) | O(n) | `v.erase(unique(v.begin(), v.end()), v.end());` |
| 9 | **min_element()** | Итератор на минимальный элемент | O(n) | `auto it = min_element(v.begin(), v.end());` |
| 10 | **max_element()** | Итератор на максимальный элемент | O(n) | `auto it = max_element(v.begin(), v.end());` |
| 11 | **accumulate()** | Сумма элементов (из `<numeric>`) | O(n) | `int sum = accumulate(v.begin(), v.end(), 0);` |
| 12 | **count()** | Подсчёт элементов | O(n) | `int cnt = count(v.begin(), v.end(), 5);` |
| 13 | **count_if()** | Подсчёт с условием | O(n) | `count_if(v.begin(), v.end(), [](int x){return x>0;});` |
| 14 | **find()** | Поиск элемента | O(n) | `auto it = find(v.begin(), v.end(), 42);` |
| 15 | **find_if()** | Поиск с условием | O(n) | `find_if(v.begin(), v.end(), [](int x){return x%2==0;});` |
| 16 | **fill()** | Заполняет значением | O(n) | `fill(v.begin(), v.end(), 0);` |
| 17 | **replace()** | Заменяет значения | O(n) | `replace(v.begin(), v.end(), 5, 10);` |
| 18 | **replace_if()** | Замена с условием | O(n) | `replace_if(v.begin(), v.end(), [](int x){return x<0;}, 0);` |
| 19 | **swap()** | Меняет местами два элемента | O(1) | `swap(a, b);` |
| 20 | **rotate()** | Циклический сдвиг | O(n) | `rotate(v.begin(), v.begin()+2, v.end());` |
| 21 | **random_shuffle()** | Перемешивает элементы | O(n) | `random_shuffle(v.begin(), v.end());` |
| 22 | **shuffle()** | Перемешивает (C++11, нужен генератор) | O(n) | `shuffle(v.begin(), v.end(), rng);` |
| 23 | **partition()** | Разделяет по условию | O(n) | `partition(v.begin(), v.end(), [](int x){return x%2==0;});` |
| 24 | **merge()** | Слияние отсортированных диапазонов | O(n+m) | `merge(v1.begin(), v1.end(), v2.begin(), v2.end(), back_inserter(result));` |
| 25 | **set_union()** | Объединение множеств | O(n+m) | `set_union(a.begin(), a.end(), b.begin(), b.end(), back_inserter(c));` |
| 26 | **set_intersection()** | Пересечение множеств | O(n+m) | `set_intersection(a.begin(), a.end(), b.begin(), b.end(), back_inserter(c));` |
| 27 | **set_difference()** | Разность множеств | O(n+m) | `set_difference(a.begin(), a.end(), b.begin(), b.end(), back_inserter(c));` |
| 28 | **is_sorted()** | Проверяет отсортированность | O(n) | `if(is_sorted(v.begin(), v.end())) {...}` |
| 29 | **all_of()** | Все элементы удовлетворяют условию | O(n) | `all_of(v.begin(), v.end(), [](int x){return x>0;});` |
| 30 | **any_of()** | Хотя бы один элемент удовлетворяет | O(n) | `any_of(v.begin(), v.end(), [](int x){return x<0;});` |

---

## 🔥 Популярные комбинации

```cpp
// Сортировка + unique (удаление дубликатов)
sort(v.begin(), v.end());
v.erase(unique(v.begin(), v.end()), v.end());

// Сортировка в обратном порядке
sort(v.rbegin(), v.rend());
// или
sort(v.begin(), v.end(), greater<int>());

// Поиск количества элементов в диапазоне [L, R]
int cnt = upper_bound(v.begin(), v.end(), R) - lower_bound(v.begin(), v.end(), L);

// Сортировка с кастомным компаратором
sort(v.begin(), v.end(), [](int a, int b) { return abs(a) < abs(b); });
```

---

## 💡 Полезные советы

### Подключение библиотек
```cpp
#include <algorithm>  // основные функции
#include <numeric>    // accumulate, iota, gcd, lcm
#include <functional> // greater, less
```

### Лямбда-функции
```cpp
// Сортировка по убыванию
sort(v.begin(), v.end(), [](int a, int b) { return a > b; });

// Фильтрация
auto it = remove_if(v.begin(), v.end(), [](int x) { return x < 0; });
v.erase(it, v.end());
```

### Работа с парами
```cpp
vector<pair<int,int>> vp;
// Сортировка по первому элементу, затем по второму
sort(vp.begin(), vp.end());

// Сортировка по второму элементу
sort(vp.begin(), vp.end(), [](auto &a, auto &b) { return a.second < b.second; });
```

---

## ⚡ Частые ошибки

1. **binary_search на несортированном массиве** ❌
   ```cpp
   // НЕПРАВИЛЬНО
   binary_search(v.begin(), v.end(), 5);
   
   // ПРАВИЛЬНО
   sort(v.begin(), v.end());
   binary_search(v.begin(), v.end(), 5);
   ```

2. **unique без erase** ❌
   ```cpp
   // НЕПРАВИЛЬНО (размер не изменится)
   unique(v.begin(), v.end());
   
   // ПРАВИЛЬНО
   v.erase(unique(v.begin(), v.end()), v.end());
   ```

3. **Итераторы после sort** ❌
   ```cpp
   auto it = find(v.begin(), v.end(), 5);
   sort(v.begin(), v.end());
   // it теперь невалиден!
   ```

---

## 📖 Дополнительные функции из `<numeric>`

| Функция | Описание | Пример |
|---------|----------|--------|
| **iota()** | Заполняет последовательными значениями | `iota(v.begin(), v.end(), 1);` // 1,2,3... |
| **gcd()** | НОД (C++17) | `int g = gcd(12, 18); // 6` |
| **lcm()** | НОК (C++17) | `int l = lcm(12, 18); // 36` |
| **partial_sum()** | Частичные суммы | `partial_sum(v.begin(), v.end(), result.begin());` |

---

**Версия стандарта:** C++11 и выше (рекомендуется компилировать с флагом `-std=c++17` или `-std=c++20`)

**Компиляция:**
```bash
g++ -std=c++17 -O2 -Wall solution.cpp -o solution
```
